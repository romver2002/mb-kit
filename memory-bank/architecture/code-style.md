---
status: draft
version: "1.0"
updated: 2026-07-05
verified_commit: ""
owner: ""
---

# Стиль кода

Соглашения {{PROJECT_NAME}} поверх автоматики. Принцип: всё, что может проверить
машина, проверяет машина — здесь только то, что она не поймает.

## Инструменты

TODO(template): заполните таблицу и подключите инструменты в CI и pre-commit.

| Инструмент | Роль | Конфиг |
|---|---|---|
| TODO(template): форматтер | форматирование — в ревью не обсуждается | TODO: путь к конфигу |
| TODO(template): линтер | стиль и типовые ошибки | |
| TODO(template): проверка типов | статическая типизация | |
| арх-линтер | границы слоёв — см. [overview.md](overview.md) | |

Пример (замените своим): ruff (format + lint, конфиг в `pyproject.toml`),
mypy в strict-режиме, import-linter; локально — `make lint`, в CI — джоба `lint`.

## Именование

- Источник имён — [глоссарий](../domain/glossary.md): сущности, статусы и операции
  в коде называются так же, как там (Ubiquitous Language). Термина нет в глоссарии —
  сначала добавь его туда, потом используй в коде.
- Один термин — одно имя: не `Client` в одном модуле и `Customer` в другом.
- Use case — глагол + существительное: `CreateOrder`, `CancelSubscription`.
- Порт и реализация: TODO(template): конвенция, напр. интерфейс `OrderRepository` →
  реализация `PgOrderRepository` (префикс технологии).
- Запрещены имена-заглушки: `Manager`, `Helper`, `Util`, `Impl`, `data2` —
  см. анти-паттерны в [patterns.md](patterns.md).
- TODO(template): регистры/кейсы под язык (snake_case/camelCase, имена файлов).

## Структура модулей

TODO(template): зафиксируйте принцип нарезки внутри слоёв и приведите пример дерева.

Пример (замените своим): внутри каждого слоя — по фичам/агрегатам
(`application/orders/`, `application/billing/`); общий код выносится в `shared/`
только после третьего использования, а не «на вырост».

## Комментарии и докстрины

- Комментарий объясняет «почему», а не «что»: «что» должно читаться из имён и структуры.
- Реализуешь бизнес-правило — ссылайся на его идентификатор/раздел в
  [business-rules.md](../domain/business-rules.md), чтобы код и база знаний сходились.
- TODO в коде — только адресный: `TODO(#123): ...`. Безадресный TODO не проходит ревью.
- Публичные порты и use cases — с докстрингом: контракт, выбрасываемые ошибки.
- Закомментированный код не хранится — удаляется, история остаётся в git.

## Чек-лист самопроверки перед PR

- [ ] Форматтер/линтер/типы/арх-линтер зелёные локально.
- [ ] Имена новых сущностей сверены с глоссарием.
- [ ] Нет безадресных TODO и закомментированного кода.
