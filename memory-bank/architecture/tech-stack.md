---
status: draft
version: "1.0"
updated: 2026-07-05
verified_commit: ""
owner: ""
---

# Технологический стек

Канонический реестр технологий {{PROJECT_NAME}} ({{STACK}}). К какому слою относится
технология — [overview.md](overview.md). Крупные выборы фиксируются через ADR —
[../decisions/README.md](../decisions/README.md).

## Реестр

Статусы: `принята` — используем по умолчанию; `испытываем` — пилот в ограниченном
объёме, для нового кода нужна явная договорённость; `выводим` — новый код не пишем,
существующий мигрируем.

| Технология | Версия | Роль | Статус |
|---|---|---|---|
| TODO(template): язык/рантайм | TODO | основной язык | принята |
| TODO(template): фреймворк | | presentation / HTTP | |
| TODO(template): БД | | основное хранилище | |
| TODO(template): ORM / драйвер БД | | infrastructure | |
| TODO(template): очередь / кеш | | infrastructure | |
| TODO(template): тест-фреймворк | | тесты | |
| TODO(template): CI | | сборка и проверки | |

Пример (замените своим):

| Технология | Версия | Роль | Статус |
|---|---|---|---|
| Python | 3.12 | основной язык | принята |
| FastAPI | 0.111 | presentation: HTTP API | принята |
| PostgreSQL | 16 | основное хранилище | принята |
| SQLAlchemy | 2.x | infrastructure: доступ к БД | принята |
| Redis | 7 | кеш, идемпотентность | испытываем |
| Celery | 5 | фоновые задачи | выводим |

## Версии

- Версии фиксируются lock-файлом: TODO(template): напр. `poetry.lock` / `package-lock.json` / `go.sum`.
- Обновление мажорной версии — отдельный PR + обновление строки реестра.
- Уязвимости зависимостей отслеживаются: TODO(template): инструмент (dependabot / renovate / pip-audit).

## Добавление новой зависимости

Прежде чем добавлять библиотеку, ответь письменно (в описании PR или в ADR):

1. Решается ли задача стандартной библиотекой или уже принятым стеком?
2. Проект живой? Поддержка, частота релизов, известные CVE.
3. Лицензия допустима? TODO(template): список допустимых лицензий (напр. MIT, Apache-2.0, BSD).
4. В какой слой попадает? Зависимости domain-слоя запрещены ([overview.md](overview.md)).
5. Насколько глубоко врастёт — какова цена отказа через год?

Процедура:

- Мелкая dev-зависимость (линтер, утилита для тестов) — обычный PR с обоснованием
  в описании.
- Всё, что влияет на архитектуру или прод-рантайм (фреймворк, БД, брокер, крупная
  библиотека), — через ADR по шаблону
  [../decisions/0000-template.md](../decisions/0000-template.md); статус `accepted`
  ставит человек (см. [governance](../_meta/governance.md)).
- После принятия — строка в реестр выше (обычно со статусом `испытываем`).
