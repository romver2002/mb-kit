---
status: draft
version: "1.0"
updated: 2026-07-05
verified_commit: ""
owner: ""
---

# Шаблон ранбука

Скопируй этот файл как `NNN-краткий-симптом.md`, заполни все разделы, удали
подсказки курсивом. Правила именования и реестр — [README.md](README.md).
Пиши команды, которые можно выполнить копипастом; проверяй их на живом
окружении при первой возможности.

---

# NNN: <Симптом одной строкой, как его видит дежурный>

**Серьёзность:** критично / деградация / косметика ·
**Алерт:** <имя правила или «нет, замечаем по жалобам»> ·
**Проверено на инциденте:** <дата>

## Симптомы

*Что видно снаружи: текст алерта, поведение для пользователя, картина на дашборде.
По этому разделу ранбук находят — пиши словами, которыми опишут проблему.*

- Пример (замените своим): алерт `HighErrorRate`: доля 5xx на `/api/*` > 5% за 5 минут.
- Пример (замените своим): пользователи видят «Что-то пошло не так» при оформлении заказа.

## Диагностика

*Команды и ссылки в порядке выполнения. Каждый шаг: что смотрим → что означает
результат. Адреса инструментов — [../observability.md](../observability.md).*

1. Открыть дашборд: <прямая ссылка> — смотрим <график>; если <картина>, то причина <X>.
2. Логи за период: `<готовый запрос к логам>` — ищем <паттерн>.
3. Пример (замените своим): `kubectl get pods -n {{PROJECT_NAME}}` — есть ли
   рестарты (`RESTARTS` > 0) за последние минуты.
4. Развилка: если <признак A> → раздел «Устранение», вариант 1;
   если <признак B> → вариант 2; иначе → «Эскалация».

## Устранение

*Пронумерованные шаги с точными командами. Для каждого разрушающего шага —
что он затрагивает и как убедиться, что помогло. Если лечение — откат релиза,
сошлись на процедуру в [../releases.md](../releases.md), не копируй её.*

1. <Команда/действие> — ожидаемый эффект: <что изменится и через сколько>.
2. Проверить, что симптом ушёл: <метрика/запрос из «Диагностики»>.
3. Если через <N минут> эффекта нет — не повторять, перейти к эскалации.

## Эскалация

*Когда перестаём чинить сами и кого зовём. Люди, каналы, SLA ответа.*

- Условие эскалации: <например, «симптом держится > 30 минут» или «затронуты платежи»>.
- Кому: TODO(template): роль/человек и канал (дежурный → тимлид → владелец системы X).
- Что передать: ссылка на этот ранбук, таймлайн действий, текущая картина метрик.

## Профилактика

*Что сделать, чтобы инцидент не повторился или ловился раньше. Каждый пункт —
задача в трекере, не пожелание.*

- [ ] <Задача с номером в трекере>: алерт/лимит/фикс первопричины.
- [ ] Обновить этот ранбук по итогам следующего срабатывания.
