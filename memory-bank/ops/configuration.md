---
status: draft
version: "1.0"
updated: 2026-07-05
verified_commit: ""
owner: ""
---

# Конфигурация {{PROJECT_NAME}}

Реестр всей конфигурации приложения: каждая переменная окружения, флаг и
настройка — одной строкой в таблице ниже.

**Правило (жёсткое):** новая env-переменная в коде = новая строка в этом файле
**в том же PR**. Ревьюер не пропускает PR, где переменная появилась, а строки нет.

**Секреты:** в этом файле живут только **имена** переменных и место хранения.
Значения секретов не попадают ни сюда, ни в код, ни в логи — правила обращения
с секретами см. [../architecture/security.md](../architecture/security.md).

## Где что задаётся

- **local** — TODO(template): например, файл `.env` (создаётся из `.env.example`,
  сам `.env` в `.gitignore`).
- **dev/stage/prod** — TODO(template): например, переменные CI/CD +
  секрет-хранилище (Vault / AWS Secrets Manager / GitHub Environments).
- Различия значений по окружениям — см. [environments.md](environments.md).

## Реестр переменных

TODO(template): заполнить по факту; примеры ниже заменить своими.

| Переменная | Назначение | Где задаётся | Секрет? |
|---|---|---|---|
| Пример (замените своим): `DATABASE_URL` | строка подключения к основной БД | local: `.env`; stage/prod: секрет-хранилище | **да** |
| Пример (замените своим): `LOG_LEVEL` | уровень логирования (`Debug`/`Information`/`Warning`) | local: `.env`; иначе: переменные CI/CD | нет |
| TODO(template): переменная | назначение | где задаётся | да/нет |

## Фиче-флаги и прочая конфигурация

TODO(template): если есть фиче-флаги / конфиг-файлы помимо env — где живут,
кто переключает, как выпиливаются отработавшие флаги. Если нет — удалить раздел.

## Чек-лист: добавляю переменную

- [ ] Имя по соглашению: TODO(template): например, `UPPER_SNAKE_CASE`
  с префиксом подсистемы (`BILLING_*`).
- [ ] Есть безопасный дефолт в коде или явная ошибка при отсутствии — что выбрано и почему.
- [ ] Добавлена в `.env.example` (для секретов — с пустым/фиктивным значением).
- [ ] Добавлена строка в реестр выше (в том же PR).
- [ ] Секрет — заведён в секрет-хранилище для всех окружений до мерджа.
- [ ] Значение не логируется (проверь вывод конфигурации при старте).

## Вопросы заполняющему

- Все ли переменные из кода уже в таблице? Сверь grep-ом по коду
  (TODO(template): команда поиска, например `grep -r "env\."` или по имени конфиг-класса).
- Что произойдёт, если переменная не задана? Ответ должен быть предсказуемым.
