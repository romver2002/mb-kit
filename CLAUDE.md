@AGENTS.md

# Специфика Claude Code

<!-- Канонические инструкции — в AGENTS.md (импортирован выше).
     Здесь только то, что относится исключительно к Claude Code. -->

- Модульные правила лежат в `.claude/rules/`; правила с `paths:` в frontmatter
  подгружаются только при работе с соответствующими файлами — детали кладём туда,
  а не сюда.
- Автопамять (команда `/memory`) — твой личный черновик: устоявшиеся знания
  переноси в `memory-bank/` через обычный PR, не оставляй важное только
  в автопамяти.
- При обновлении CLAUDE.md / AGENTS.md помни: они входят в бюджет каждой сессии —
  новые разделы добавляй в memory-bank и ссылайся, а не расширяй эти файлы.
